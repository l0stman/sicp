\documentclass[a4paper,12pt]{article}
\usepackage{listings}
\lstset{language=Lisp}

\begin{document}
\begin{lstlisting}
(define (last-pair l)
  (if (null? (cdr l))
      l
      (last-pair (cdr l))))
\end{lstlisting}
\end{document}
