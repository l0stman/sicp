\documentclass[a4paper,12pt]{article}
\usepackage{listings}
\lstset{language=Lisp}

\begin{document}
\begin{lstlisting}
(define (same-parity x . l)
  (cons x
        (filter (lambda (y)
                  (if (odd? x)
                      (odd? y)
                      (even? y)))
                l)))
        
\end{lstlisting}
\end{document}
