\documentclass[a4paper,12pt]{article}
\usepackage{listings}
\lstset{language=Lisp}

\begin{document}
\begin{lstlisting}
(define (smooth f)
  (average (f (- x dx))
	   (f (+ x dx))))
(define (n-fold f n)
  (repeated smooth n))
\end{lstlisting}
\end{document}
